export const CONNECT_TIMEOUT_MS = 20000;
export const CHECK_TIMEOUT_MS = 10000;
export const TAILOR_TIMEOUT_MS = 20000;
export const EMPTY_RESUME_PREVIEW = `<!doctype html>
<html>
<body style="font-family: Arial, sans-serif; padding: 24px; color: #475569;">
  <div style="max-width: 520px;">
    <h2 style="margin: 0 0 8px; font-size: 18px;">Resume preview</h2>
    <p style="margin: 0; font-size: 13px; line-height: 1.5;">
      Generate a tailored resume to see the template preview. Make sure this profile has a resume template assigned.
    </p>
  </div>
</body>
</html>`;
